name: experiment
on:
  workflow_dispatch:
jobs:
  band:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node band-experiment.mjs
